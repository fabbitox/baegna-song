const title = [{"singer": "배그나(은하수)", "songs": ["나란여자", "내사랑구례", "너라는마법", "뚝뚝뚝", "못참아",
	"소꿉친구", "숨겨온마음", "안돼요돼요", "애태우다", "옛사랑", "이말전할수있을까", "좋아요", "팽이같은인생"]},
    {"singer": "가비엔제이", "songs": ["그래도살아가겠지", "널사랑하는데", "눈사람", "반대편에서서", "연애소설",
        "이별주의보", "절애", "해바라기", "Everyday/에브리데이", "Happiness/해피니스", "LOVE ALL/러브올"]},
    {"singer": "강민경", "songs": ["사랑해서그래"]}, {"singer": "거미", "songs": ["그대돌아오면", "그대라서",
        "기억해줘요내모든날과그때를", "날그만잊어요", "너의하루는좀어때", "눈꽃", "아니", "어른아이", "죽어도사랑해",
        "지워져", "친구라도될걸그랬어", "통증", "혼자만하는사랑", "You are my everything/유아마이에브리띵"]},
    {"singer": "김길중", "songs": ["사랑했지만"]}, {"singer": "김나영", "songs": ["꺼내본다", "니가없다면", "솔직하게말해서나",
        "헤어질수밖에", "홀로"]}, {"singer": "김대명", "songs": ["가을우체국앞에서(슬기로운의사생활 시즌2 OST)"]},
    {"singer": "김동희", "songs": ["그대를그대를", "사랑은하지마", "사랑을몰랐죠", "썸데이", "이런다고", "이제야",
        "헤어질땐말없이"]}, {"singer": "김범수", "songs": ["끝사랑"]}, {"singer": "김보경",
        "songs": ["Suddenly/서든리(시티헌터 OST)"]}, {"singer": "김아중", "songs": ["마리아"]},
    {"singer": "김윤아(자우림)", "songs": ["길(시그널 OST)", "스물다섯스물하나", "야상곡"]}, {"singer": "김진호",
        "songs": ["가족사진"]}, {"singer": "김현정", "songs": ["그녀와의이별", "멍"]}, {"singer": "노라조", "songs": ["형"]},
    {"singer": "노을", "songs": ["늦은밤너의집앞골목길에서", "청혼"]}, {"singer": "도원경", "songs": ["다시사랑한다면"]},
    {"singer": "도은영", "songs": ["그래도사랑해요"]}, {"singer": "다비치", "songs": ["거북이", "그냥안아달란말야",
        "그대니까요", "나의오랜연인에게", "남자도우나요", "너라서", "너없는시간들", "너에게못했던내마지막말은",
        "또운다또", "모르시나요", "미워도사랑하니까", "사고쳤어요", "사랑과전쟁", "시간아멈춰라", "여성시대",
        "오늘도그리워그리워", "이사랑", "화이트/WHITE", "8282"]}, {"singer": "더리슨(솔지x김나영x케이시x승희xHYNN)",
        "songs": ["느린이별"]}, {"singer": "란", "songs": ["어쩌다가"]}, {"singer": "럼블피쉬", "songs": ["비와당신"]},
    {"singer": "린", "songs": ["곰인형", "돌팔매/사랑의콜센타", "사랑했잖아", "시간을거슬러", "실화", "이별살이",
        "자기야여보야사랑아"]}, {"singer": "마마무", "songs": ["넌 is 뭔들", "데칼코마니"]}, {"singer": "마야",
        "songs": ["나를외치다", "못다핀꽃한송이", "진달래꽃"]}, {"singer": "마크툽", "songs": ["오늘도빛나는너에게"]},
    {"singer": "먼데이키즈", "songs": ["그대여"]}, {"singer": "모세", "songs": ["사랑인걸"]}, {"singer": "박미경",
        "songs": ["기억속의먼그대에게", "민들레홀씨되어"]}, {"singer": "박보람(동물원)", "songs": ["혜화동(쌍문동)"]},
    {"singer": "박혜원", "songs": ["LET ME OUT/렛미아웃"]}, {"singer": "백아연", "songs": ["이럴거면그러지말지"]},
    {"singer": "백지영", "songs": ["거짓말이라도해서널보고싶어", "그대의마음", "나쁜사람(황진이 OST)",
        "다시는사랑하지않고이별에아파하기싫어", "사랑안해", "사랑하나면돼", "잊지말아요", "총맞은것처럼"]},
    {"singer": "버블시스터즈", "songs": ["그렇게사랑하고그렇게웃었습니다", "버블송", "애원"]}, {"singer": "버즈",
        "songs": ["겁쟁이", "나에게로떠나는여행"]}, {"singer": "베이지", "songs": ["그리워그리워서", "달에지다"]},
    {"singer": "벤", "songs": ["안갯길", "여자이니까"]}, {"singer": "보보", "songs": ["늦은후회"]}, {"singer": "빅마마",
        "songs": ["배반", "소리", "연", "체념", "하루만더"]}, {"singer": "서문탁", "songs": ["사랑결코시들지않는"]},
    {"singer": "서영은", "songs": ["내안의그대", "너에게로또다시", "혼자가아닌나"]}, {"singer": "소녀시대",
        "songs": ["다시만난세계", "소녀시대", "Kissing You/키싱유"]}, {"singer": "수지", "songs": ["겨울아이"]},
    {"singer": "소찬휘", "songs": ["일엽락"]}, {"singer": "소향", "songs": ["가슴만알죠", "나언젠간떠날거야(모아나 OST)",
        "바람의노래/조용필", "오직단하나(마의 OST)"]}, {"singer": "손디아", "songs": ["어른", "혼잣말"]},
    {"singer": "송하예", "songs": ["니소식", "새사랑"]}, {"singer": "송이한", "songs": ["밝게빛나는별이되어비춰줄게"]},
    {"singer": "신승훈", "songs": ["미소속에비친그대"]}, {"singer": "신현희와김루트", "songs": ["오빠야"]},
    {"singer": "심규선", "songs": ["부디", "아라리", "야래향", "어떤날도어떤말도", "월령"]}, {"singer": "씨야",
        "songs": ["구두", "그사람이나를사랑해요", "미워요", "미친사랑의노래", "사랑의인사", "슬픈발걸음(구두2)", "얼음인형",
        "여인의향기", "정", "Promise U/프로미스유", "The Last/더라스트"]}, {"singer": "아이오아이/IOI", "songs": ["소나기"]},
    {"singer": "아이유/IU", "songs": ["가을아침", "개여울", "나만몰랐던이야기", "내손을잡아", "너의의미", "무릎",
        "미리크리스마스", "미아", "밤편지", "사랑이지나가면", "이름에게", "있잖아", "잔소리", "좋은날", "Love poem/러브포엠",
        "Rain Drop/레인드롭"]}, {"singer": "악동뮤지션", "songs": ["어떻게이별까지사랑하겠어,널사랑하는거지"]},
    {"singer": "안예은", "songs": ["상사화", "홍연"]}, {"singer": "알리", "songs": ["서약", "펑펑", "365일"]},
    {"singer": "야다", "songs": ["이미슬픈사랑", "진혼"]}, {"singer": "에일리", "songs": ["잠시안녕처럼",
        "첫눈처럼너에게가겠다"]}, {"singer": "오렌지캬라멜", "songs": ["아잉"]}, {"singer": "왁스", "songs": ["날떠난이유",
        "머니", "부탁해요", "사랑이다그런거니까", "엄마의일기", "오빠", "화장을고치고", "황혼의문턱"]}, {"singer": "유미",
        "songs": ["별", "사랑은언제나목마르다"]}, {"singer": "유성은", "songs": ["그대꽃"]}, {"singer": "유정석",
        "songs": ["질풍가도"]}, {"singer": "유재하", "songs": ["사랑하기때문에"]}, {"singer": "유주",
        "songs": ["호랑수월가(나와호랑이님 OST)"]}, {"singer": "윤미래", "songs": ["시간이흐른뒤",
        "ALWAYS/얼웨이즈올웨이즈"]}, {"singer": "윤하", "songs": ["기다리다", "비밀번호486", "오늘헤어졌어요", "혜성"]},
    {"singer": "이루", "songs": ["까만안경"]}, {"singer": "이브", "songs": ["너그럴때면"]}, {"singer": "이소라",
        "songs": ["기억해줘", "바람이분다", "제발", "처음느낌그대로"]}, {"singer": "이수영", "songs": ["라라라",
        "사랑하면할수록", "얼마나좋을까", "여전히입술을깨물죠", "휠릴리", "Grace/그레이스", "I Believe/아이빌리브"]},
    {"singer": "이승기", "songs": ["내여자라니까", "되돌리다", "삭제", "여행을떠나요"]}, {"singer": "이안",
        "songs": ["물고기자리"]}, {"singer": "이예준", "songs": ["그날에나는맘이편했을까", "그리워그리워", "미친소리"]},
    {"singer": "이은미", "songs": ["녹턴", "애인있어요"]}, {"singer": "이재성", "songs": ["그집앞"]}, {"singer": "이하이",
        "songs": ["한숨"]}, {"singer": "인순이", "songs": ["아버지"]}, {"singer": "임정희", "songs": ["눈물이안났어",
        "사랑아가지마"]}, {"singer": "임재범", "songs": ["고해", "낙인(추노 OST)"]}, {"singer": "잔나비",
        "songs": ["주저하는연인들을위해"]}, {"singer": "장나라", "songs": ["Sweet Dream/스윗드림"]}, {"singer": "장윤정",
        "songs": ["꽃", "너떠난후에", "다시한번", "당신편", "돼지토끼", "목포행완행열차", "바람길", "반창고", "벚꽃길",
        "불나비", "사랑아", "사랑참", "세월아", "송인", "애가타", "약속", "어머나", "어부바", "옆집누나", "올래", "운명에게",
        "장윤정트위스트", "좋은당신", "짠짜라", "첫사랑", "초혼", "케세라세라", "콩깍지", "해바라기"]}, {"singer": "장혜진",
        "songs": ["아름다운날들"]}, {"singer": "정경화", "songs": ["지상에서영원으로"]}, {"singer": "정준일",
        "songs": ["안아줘"]}, {"singer": "제아", "songs": ["미아"]}, {"singer": "조항조", "songs": ["후"]},
    {"singer": "지동국", "songs": ["돌아가는길"]}, {"singer": "지아", "songs": ["가끔", "감기때문에", "그대만보여요",
        "그런다면서", "그런줄알았어", "그립습니다", "난행복해", "낡은옷", "너를기억한다", "누군가의무엇이되어", "눈물",
        "더만나봤자뭐해", "먼발치", "물끄러미", "물론", "바이올린", "사랑만알아서", "사랑해미안해", "술한잔해요",
        "술한잔해요오늘", "엄마미안해요", "우두커니", "웃게해줄게", "웃어줄래", "웃음만", "인형", "헤어진첫날",
        "The Day/더데이", "Only One/온리원"]}, {"singer": "차지연", "songs": ["살다보면"]}, {"singer": "케이시",
        "songs": ["가을밤떠난너", "그때가좋았어", "행복하니"]}, {"singer": "코요태", "songs": ["우리의꿈(원피스)"]},
    {"singer": "태연", "songs": ["그대라는시", "그리고하나", "내게들려주고싶은말", "들리나요", "만약에", "미치게보고싶은",
        "불티", "사계", "사랑해요", "숨겨진세상(겨울왕국2 OST)", "제주도의푸른밤", "Curtain Call/커튼콜", "Fine/파인",
        "I/아이", "Rain/레인"]}, {"singer": "페이지", "songs": ["이별이오지못하게"]}, {"singer": "폴킴",
        "songs": ["모든날모든순간"]}, {"singer": "플라이 투 더 스카이", "songs": ["가슴아파도"]}, {"singer": "홍진영",
        "songs": ["그대오는날", "눈물비", "달의그림자/클랜즈:달의그림자 OST", "따르릉", "부기맨", "사랑은꽃잎처럼",
        "사랑은다이러니", "사랑의배터리", "사랑의와이파이", "사랑이좋아", "산다는건", "안돼요", "엄지척", "오늘밤에",
        "잘가라"]}, {"singer": "izi/이지", "songs": ["응급실/쾌걸춘향 OST"]}, {"singer": "SG워너비", "songs": ["살다가"]},
        {"singer": "Yarz/야즈", "songs": ["괜찮아요난(올인 OST)"]}];
const trot = [{"singer": "강민주", "songs": ["회룡포"]}, {"singer": "강진", "songs": ["땡벌", "붓"]},
    {"singer": "곽지은", "songs": ["꽃으로"]}, {"singer": "금잔디", "songs": ["나를살게하는사랑", "서울가살자",
        "신사랑고개", "엄마의노래", "여여", "오라버니", "일편단심", "초롱새"]}, {"singer": "김다나",
        "songs": ["자시삼경"]}, {"singer": "김다비", "songs": ["주라주라"]}, {"singer": "김란영",
        "songs": ["가인"]}, {"singer": "김소유", "songs": ["숫자인생"]}, {"singer": "김수희",
        "songs": ["남행열차", "애모"]}, {"singer": "김연자", "songs": ["블링블링", "수은등", "아모르파티",
        "타이틀", "10분내로"]}, {"singer": "김용임", "songs": ["나이야가라", "내장산", "부초같은인생", "빙빙빙",
        "사랑님", "열두줄", "울지마라세월아", "훨훨훨"]}, {"singer": "김의영", "songs": ["용두산엘레지"]},
    {"singer": "김의영, 윤태화", "songs": ["비익조"]}, {"singer": "김지애", "songs": ["얄미운사람"]},
    {"singer": "김태연(이태호)", "songs": ["간대요글쎄"]}, {"singer": "김혜연", "songs": ["예쁜여우", "토요일밤에"]},
    {"singer": "나훈아", "songs": ["고장난벽시계", "내게애인이생겼어요", "사내", "사모", "잡초", "테스형", "홍시"]},
    {"singer": "남진", "songs": ["나야나", "님과함께", "둥지", "빈잔"]}, {"singer": "대성", "songs": ["대박이야"]},
    {"singer": "류계영", "songs": ["인생"]}, {"singer": "문희옥", "songs": ["사랑의거리", "평행선"]},
    {"singer": "박군", "songs": ["한잔해"]}, {"singer": "박상철", "songs": ["무조건", "빵빵", "자옥아",
        "항구의남자", "황진이"]}, {"singer": "박현빈", "songs": ["곤드레만드레", "샤방샤방"]}, {"singer": "백난아",
        "songs": ["찔레꽃"]}, {"singer": "백설희", "songs": ["봄날은간다"]}, {"singer": "서정아",
        "songs": ["비비각시"]}, {"singer": "서주경", "songs": ["쓰러집니다"]}, {"singer": "서지오",
        "songs": ["하니하니"]}, {"singer": "소유미", "songs": ["알랑가몰라"]}, {"singer": "송가인",
        "songs": ["가인이어라", "무명배우", "서울의달", "엄마아리랑", "트로트가나는좋아요"]}, {"singer": "송대관",
        "songs": ["네박자", "차표한장"]}, {"singer": "숙행", "songs": ["가시리"]}, {"singer": "신유",
        "songs": ["나쁜남자", "시계바늘", "일소일소일노일노", "잠자는공주"]}, {"singer": "심수봉",
        "songs": ["그때그사람", "미워요", "백만송이장미", "비나리", "사랑밖엔난몰라"]}, {"singer": "안소미",
        "songs": ["술한잔"]}, {"singer": "LPG/엘피지", "songs": ["사랑의초인종"]}, {"singer": "오승근",
        "songs": ["내나이가어때서", "있을때잘해"]}, {"singer": "오은주", "songs": ["돌팔매"]}, {"singer": "요요미",
        "songs": ["이오빠뭐야"]}, {"singer": "우연이", "songs": ["우연히"]}, {"singer": "유산슬",
        "songs": ["사랑의재개발", "합정역5번출구"]}, {"singer": "유지나", "songs": ["미운사내"]}, {"singer": "유진표",
        "songs": ["천년지기"]}, {"singer": "윤수일", "songs": ["아파트"]}, {"singer": "윤수현", "songs": ["꽃길",
        "손님온다", "천태만상"]}, {"singer": "양지은", "songs": ["붓"]}, {"singer": "이미자", "songs": ["동백아가씨",
        "섬마을선생님"]}, {"singer": "이이경", "songs": ["칼퇴근"]}, {"singer": "이자연", "songs": ["당신의의미",
        "찰랑찰랑"]}, {"singer": "이효정", "songs": ["우리어머니"]}, {"singer": "이혜리", "songs": ["모르나봐",
        "자갈치아지매"]}, {"singer": "임주리", "songs": ["립스틱짙게바르고"]}, {"singer": "전미경", "songs": ["장녹수"]},
    {"singer": "정은실", "songs": ["은실이"]}, {"singer": "정의송", "songs": ["님이여"]}, {"singer": "조은새",
        "songs": ["하트하트"]}, {"singer": "주현미", "songs": ["그대를불러봅니다", "길", "또만났네요", "러브레터", "사랑가",
        "신사동그사람", "아버지", "여인의눈물", "여정", "정말좋았네", "짝사랑"]}, {"singer": "진미령",
        "songs": ["미운사랑"]}, {"singer": "진성", "songs": ["가지마", "보릿고개", "안동역에서", "울엄마",
        "태클을걸지마"]}, {"singer": "채희", "songs": ["바람의소원"]}, {"singer": "최대성", "songs": ["혹시"]},
    {"singer": "최진희", "songs": ["꼬마인형", "사랑의미로", "천상재회"]}, {"singer": "추가열", "songs": ["소풍같은인생"]},
    {"singer": "태진아", "songs": ["당신의눈물", "동반자", "사랑은아무나하나", "아줌마", "애인", "진진자라"]},
    {"singer": "편승엽", "songs": ["찬찬찬"]}, {"singer": "한혜진", "songs": ["갈색추억", "너는내남자", "마지막연인",
        "서울의밤"]}, {"singer": "홍자", "songs": ["눈물의술잔"]}, {"singer": "홍지윤", "songs": ["오라"]},];
const mrTrot = [{"singer": "김호중", "songs": ["너나나나", "태클을걸지마", "할무니"]}, {"singer": "사랑의콜센타",
        "songs": ["날보러와요"]}, {"singer": "영탁", "songs": ["니가왜거기서나와", "막걸리한잔", "우리정말나쁘다",
        "이불", "추억으로가는당신"]}, {"singer": "이찬원", "songs": ["시절인연", "진또배기"]}, {"singer": "임영웅",
        "songs": ["바램", "별빛같은나의사랑아", "보랏빛엽서", "사랑은늘도망가/이문세", "사랑이이런건가요", "어느날문득",
        "이제나만믿어요", "일편단심민들레야"]}, {"singer": "장민호", "songs": ["내이름아시죠"]}, {"singer": "정동원",
        "songs": ["여백", "청춘", "청포도사나이", "파트너/남진장민호"]},];
const old = [{"singer": "구창모", "songs": ["희나리"]}, {"singer": "김광석", "songs": ["너무아픈사랑은사랑이아니었음을",
        "바람이불어오는곳", "서른즈음에", "어느60대노부부이야기", "이등병의편지", "잊어야한다는마음으로"]},
    {"singer": "김연숙", "songs": ["그날"]}, {"singer": "김추자", "songs": ["님은먼곳에", "무인도"]},
    {"singer": "김혜림", "songs": ["날위한이별"]}, {"singer": "나미", "songs": ["빙글빙글", "슬픈인연"]},
    {"singer": "박성신", "songs": ["한번만더"]}, {"singer": "박정수", "songs": ["그대품에잠들었으면"]},
    {"singer": "배따라기", "songs": ["비와찻잔사이"]}, {"singer": "뱅크", "songs": ["가질수없는너"]},
    {"singer": "변진섭", "songs": ["너에게로또다시", "숙녀에게"]}, {"singer": "서울시스터즈(방실이)", "songs": ["첫차"]},
    {"singer": "석미경", "songs": ["물안개"]}, {"singer": "별", "songs": ["12월32일"]}, {"singer": "소리새",
        "songs": ["그대그리고나"]}, {"singer": "양수경", "songs": ["당신은어디있나요", "사랑은창밖에빗물같아요",
        "이별의끝은어디인가요"]}, {"singer": "양희은", "songs": ["사랑그쓸쓸함에대하여", "이루어질수없는사랑",
        "하얀목련"]}, {"singer": "우순실", "songs": ["잃어버린우산"]}, {"singer": "윤도현",
        "songs": ["가을우체국앞에서(슬기로운의사생활 시즌2 OST)"]}, {"singer": "이문세", "songs": ["광화문연가",
        "소녀", "옛사랑", "휘파람"]}, {"singer": "이선희", "songs": ["그중에그대를만나", "나항상그대를",
        "라일락이질때", "아름다운강산", "알고싶어요", "여우비", "인연", "추억의책장을넘기면"]}, {"singer": "이용",
        "songs": ["잊혀진계절"]}, {"singer": "이은하", "songs": ["돌이키지마", "미소를띄우며나를보낸그모습처럼",
        "봄비"]}, {"singer": "임희숙", "songs": ["내하나의사람은가고"]}, {"singer": "장덕", "songs": ["뒤늦은후회"]},
    {"singer": "장연주", "songs": ["여가"]}, {"singer": "장혜리", "songs": ["내게남은사랑을드릴게요", "추억의발라드"]},
    {"singer": "전영록", "songs": ["저녁놀"]}, {"singer": "전유나", "songs": ["그사람", "너를사랑하고도"]},
    {"singer": "최용준", "songs": ["아마도그건"]}, {"singer": "정훈희", "songs": ["꽃밭에서"]},
    {"singer": "조용필", "songs": ["그겨울의찻집"]}, {"singer": "주니퍼", "songs": ["하늘끝에서흘린눈물"]},
    {"singer": "채은옥", "songs": ["빗물"]}, {"singer": "햇빛촌", "songs": ["유리창엔비"]}, {"singer": "혜령",
        "songs": ["슬픔을참는세가지방법"]}, {"singer": "혜은이", "songs": ["당신은모르실거야", "비가", "새벽비",
        "열정", "옛사랑의돌담길", "제3한강교", "진짜진짜좋아해"]}];
const other = [{"singer": "등려군", "songs": ["월량대표아적심(첨밀밀 OST)"]}, {"singer": "Idina Menzel",
    "songs": ["Let It Go(겨울왕국 OST)/렛잇고"]}, {"singer": "Naomi Scott/나오미스콧",
    "songs": ["Speechless(알라딘 OST)/스피치리스"]}, {"singer": "", "songs": ["검정고무신", "달려라하니",
    "세일러문", "시대를초월한마음(이누야샤)", "안녕디지몬", "우리는모두친구(포켓몬)"]},];

let songsPerRow; // 한 줄에 몇 개까지 나열할 건지

let allHtml = null;

function changecolor(x) {// mouseenter
    x.style.color = "#000000";
}
function returncolor(x) {// mouseleave
    x.style.color = "#ffffff";
}

function cleartext() {// 초기화
    document.getElementById("searchBox").value = null;
    document.getElementById("show").innerHTML = allHtml;
}

function distinguish() {// 모바일 환경 고려
    let filter = "win16|win32|win64|mac|macintel";
    if (navigator.platform) {
        if (filter.indexOf(navigator.platform.toLowerCase()) < 0) {
            songsPerRow = 2;
        }
        else {
            songsPerRow = 6;
        }
    }
}

function spanCalc(x) {// 몇 줄인지 계산
    let remainder = x % songsPerRow;
    if (remainder == 0) {
        return x / songsPerRow;
    }
    else return (x - remainder) / songsPerRow + 1;
}

function showAll() {// 목록 전부 보여주기
    distinguish();

    titleTable();
    htmlText += "<br>";
    trotTable();
    htmlText += "<br>";
    mrTrotTable();
    htmlText += "<br>";
    oldTable();
    htmlText += "<br>";
    otherTable();

    document.getElementById("show").innerHTML = htmlText;
    allHtml = htmlText;
}

function otherTable() {
    htmlText += "<table align=\"center\" <caption><h3 id=\"other\">팝, 애니</h3></caption>";
    for (let i = 0; i < other.length; i++) {
        let songslen = other[i].songs.length;
        let spannum = spanCalc(songslen);
        htmlText += "<tr><td rowspan=\"" + spannum + "\">" + other[i].singer.split("/")[0] + "</td>"; // 가수
        for (let j = 0; j < songslen; j++) { // 노래들
            htmlText += "<td ";
            if (songsPerRow == 2) {
                htmlText += "style=\"width:34%;\"";
            }
            else {
                htmlText += "style=\"width:14.5%;\"";
            }
            htmlText += ">" + other[i].songs[j].split("/")[0] + "</td>";
            if (j % songsPerRow == songsPerRow - 1) {
                htmlText += "</tr>";
                if (j != songslen - 1) {
                    htmlText += "<tr>";
                }
            }
        }
        if (songslen % songsPerRow != 0) {
            htmlText += "</tr>";
        }
    }
    htmlText += "</table>";
}

function oldTable() {
    htmlText += "<table align=\"center\" <caption><h3 id=\"old\">옛 가요</h3></caption>";
    for (let i = 0; i < old.length; i++) {
        let songslen = old[i].songs.length;
        let spannum = spanCalc(songslen);
        htmlText += "<tr><td rowspan=\"" + spannum + "\">" + old[i].singer.split("/")[0] + "</td>"; // 가수
        for (let j = 0; j < songslen; j++) { // 노래들
            htmlText += "<td ";
            if (songsPerRow == 2) {
                htmlText += "style=\"width:34%;\"";
            }
            else {
                htmlText += "style=\"width:14.5%;\"";
            }
            htmlText += ">" + old[i].songs[j].split("/")[0] + "</td>";
            if (j % songsPerRow == songsPerRow - 1) {
                htmlText += "</tr>";
                if (j != songslen - 1) {
                    htmlText += "<tr>";
                }
            }
        }
        if (songslen % songsPerRow != 0) {
            htmlText += "</tr>";
        }
    }
    htmlText += "</table>";
}

function mrTrotTable() {
    htmlText += "<table align=\"center\" <caption><h3 id=\"mrTrot\">미스터트롯</h3></caption>";
    for (let i = 0; i < mrTrot.length; i++) {
        let songslen = mrTrot[i].songs.length;
        let spannum = spanCalc(songslen);
        htmlText += "<tr><td rowspan=\"" + spannum + "\">" + mrTrot[i].singer.split("/")[0] + "</td>"; // 가수
        for (let j = 0; j < songslen; j++) { // 노래들
            htmlText += "<td ";
            if (songsPerRow == 2) {
                htmlText += "style=\"width:34%;\"";
            }
            else {
                htmlText += "style=\"width:14.5%;\"";
            }
            htmlText += ">" + mrTrot[i].songs[j].split("/")[0] + "</td>";
            if (j % songsPerRow == songsPerRow - 1) {
                htmlText += "</tr>";
                if (j != songslen - 1) {
                    htmlText += "<tr>";
                }
            }
        }
        if (songslen % songsPerRow != 0) {
            htmlText += "</tr>";
        }
    }
    htmlText += "</table>";
}

function trotTable() {
    htmlText += "<table align=\"center\" <caption><h3 id=\"trot\">트로트</h3></caption>";
    for (let i = 0; i < trot.length; i++) {
        let songslen = trot[i].songs.length;
        let spannum = spanCalc(songslen);
        htmlText += "<tr><td rowspan=\"" + spannum + "\">" + trot[i].singer.split("/")[0] + "</td>"; // 가수
        for (let j = 0; j < songslen; j++) { // 노래들
            htmlText += "<td ";
            if (songsPerRow == 2) {
                htmlText += "style=\"width:34%;\"";
            }
            else {
                htmlText += "style=\"width:14.5%;\"";
            }
            htmlText += ">" + trot[i].songs[j].split("/")[0] + "</td>";
            if (j % songsPerRow == songsPerRow - 1) {
                htmlText += "</tr>";
                if (j != songslen - 1) {
                    htmlText += "<tr>";
                }
            }
        }
        if (songslen % songsPerRow != 0) {
            htmlText += "</tr>";
        }
    }
    htmlText += "</table>";
}

function titleTable() {
    htmlText = "<table id=\"baegna\" align=\"center\">";
    for (let i = 0; i < title.length; i++) {
        let songslen = title[i].songs.length;
        let spannum = spanCalc(songslen);
        htmlText += "<tr><td rowspan=\"" + spannum + "\"";
        if (i == 0) {
            htmlText += " style=\"background-color: rgb(50, 250, 150);\"";
        }
        htmlText += ">" + title[i].singer.split("/")[0] + "</td>"; // 가수
        for (let j = 0; j < songslen; j++) { // 노래들
            htmlText += "<td ";
            if (songsPerRow == 2) {
                htmlText += "style=\"width: 34%;\"";
            }
            else {
                htmlText += "style=\"width: 14.5%;\"";
            }
            htmlText += ">" + title[i].songs[j].split("/")[0] + "</td>";
            if (j % songsPerRow == songsPerRow - 1) {
                htmlText += "</tr>";
                if (j != songslen - 1) {
                    htmlText += "<tr>";
                }
            }
        }
        if (songslen % songsPerRow != 0) {
            htmlText += "</tr>";
        }
    }
    htmlText += "</table>";
}

function search(str) {// 검색 결과 띄우기
    if (str == '') {// 빈 칸이면 전체 보여줌
        document.getElementById("show").innerHTML = allHtml;
    }
    else {
        let searchedSinger = "<table align=\"center\">";
        let searchedSong = "<table align=\"center\">";
        let words = str.trim().split(/\s+/);// 띄어쓰기 검색 고려, 단어로 나눔
        let strlen = [];// 단어의 길이
        let numwords = words.length;// 단어 수
        for (let i = 0; i < numwords; i++) {// 각 단어 길이 구하기
            let wordlen = words[i].length;
            strlen.push(wordlen);
        }
        ({ searchedSinger, searchedSong } = titleSearch(numwords, words, searchedSinger, strlen, searchedSong));
        ({ searchedSinger, searchedSong } = trotSearch(numwords, words, searchedSinger, strlen, searchedSong));
        ({ searchedSinger, searchedSong } = mrTrotSearch(numwords, words, searchedSinger, strlen, searchedSong));
        ({ searchedSinger, searchedSong } = oldSearch(numwords, words, searchedSinger, strlen, searchedSong));
        ({ searchedSinger, searchedSong } = otherSearch(numwords, words, searchedSinger, strlen, searchedSong));
        document.getElementById("show").innerHTML = searchedSinger + searchedSong;
    }
}

function otherSearch(numwords, words, searchedSinger, strlen, searchedSong) {
    for (let i = 0; i < other.length; i++) {
        let singer = other[i].singer; // 가수
        let songs = other[i].songs; // 노래들
        let songslen = songs.length; // 노래 수
        let spannum = spanCalc(songslen);
        let searchResult = []; // 검색 결과
        for (let j = 0; j < numwords; j++) { // 가수 검색
            let wordSearch = singer.toLowerCase().search(words[j].toLowerCase());
            if (wordSearch != -1) {
                searchResult.push(wordSearch);
            }
        }
        singer = singer.split("/")[0];
        if (numwords == searchResult.length) { // 가수에서 단어들이 다 검색되면
            searchedSinger += "<tr><td rowspan=\"" + spannum + "\" ";
            if (songsPerRow == 2) {
                searchedSinger += "style=\"width:32%;\"";
            }
            else {
                searchedSinger += "style=\"width:13%;\"";
            }
            searchedSinger += ">" + singer.substring(0, searchResult[0]); // 첫 단어 검색 전
            let pos;
            let index;
            for (let j = 0; j < numwords; j++) {
                if (j == 0) {
                    pos = searchResult[0];
                    index = 0;
                    searchedSinger += "<b>" + singer.substr(pos, strlen[0]) + "</b>"; // 처음 검색된 부분
                }
                else {
                    if (searchResult[j] > pos) {
                        pos = searchResult[j];
                        index = j;
                        searchedSinger += "<b>" + singer.substr(pos, strlen[j]) + "</b>"; // 다음 검색된 부분
                    }
                }
                if (j != numwords - 1) { // 마지막 단어가 아니면
                    if (searchResult[j] + strlen[j] < searchResult[j + 1]) { // 다음 단어 검색 전
                        searchedSinger += singer.substring(searchResult[j] + strlen[j], searchResult[j + 1]);
                    }
                }
            }
            searchedSinger += singer.substr(pos + strlen[index]) + "</td>"; // 마지막 단어 검색 후

            for (let j = 0; j < songslen; j++) {
                let songResult = []; // 가수 노래 검색
                let index = [];
                for (let k = 0; k < numwords; k++) { // 노래에서도 검색되는지
                    let songSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (songSearch != -1) {
                        songResult.push(songSearch);
                        index.push(k);
                    }
                }
                let song = songs[j].split("/")[0];
                let inlen = index.length;
                if (inlen != 0) { // 노래에서 검색된 게 있음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song.substring(0, songResult[0]);
                    let pos;
                    let ind;
                    for (let k = 0; k < inlen; k++) {
                        if (k == 0) {
                            pos = songResult[0];
                            ind = index[0];
                            searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 처음 검색된 부분
                        }
                        else {
                            if (songResult[k] > pos) {
                                pos = songResult[k];
                                ind = index[k];
                                searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 다음 검색된 부분
                            }
                        }
                        if (k != inlen - 1) {
                            let inK = index[k];
                            if (songResult[k] + strlen[inK] < songResult[k + 1]) {
                                searchedSinger += song.substring(songResult[k] + strlen[inK], songResult[k + 1]);
                            }
                        }
                    }
                    searchedSinger += song.substr(pos + strlen[ind]) + "</td>";
                }
                else { // 없음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song + "</td>";
                }
                if (j % songsPerRow == songsPerRow - 1) {
                    searchedSinger += "</tr>";
                    if (j != songslen - 1) {
                        searchedSinger += "<tr>";
                    }
                }
            }
            if (songslen % songsPerRow != 0) {
                searchedSinger += "</tr>";
            }
        }
        else {
            for (let j = 0; j < songslen; j++) {
                searchResult = [];
                for (let k = 0; k < numwords; k++) {
                    let wordSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (wordSearch != -1) {
                        searchResult.push(wordSearch);
                    }
                }
                if (numwords == searchResult.length) {
                    let song = songs[j].split("/")[0];
                    searchedSong += "<tr><td>" + singer + "</td>" + "<td>" + song.substring(0, searchResult[0]);
                    let pos;
                    let index;
                    for (let k = 0; k < numwords; k++) {
                        if (k == 0) {
                            pos = searchResult[0];
                            index = 0;
                            searchedSong += "<b>" + song.substr(pos, strlen[0]) + "</b>";
                        }
                        else {
                            if (searchResult[k] > pos) {
                                pos = searchResult[k];
                                index = k;
                                searchedSong += "<b>" + song.substr(pos, strlen[k]) + "</b>";
                            }
                        }
                        if (k != numwords - 1) {
                            if (searchResult[k] + strlen[k] < searchResult[k + 1]) {
                                searchedSong += song.substring(searchResult[k] + strlen[k], searchResult[k + 1]);
                            }
                        }
                    }
                    searchedSong += song.substr(pos + strlen[index]) + "</td></tr>";
                }
            }
        }
    }
    return { searchedSinger, searchedSong };
}

function oldSearch(numwords, words, searchedSinger, strlen, searchedSong) {
    for (let i = 0; i < old.length; i++) {
        let singer = old[i].singer; // 가수
        let songs = old[i].songs; // 노래들
        let songslen = songs.length; // 노래 수
        let spannum = spanCalc(songslen);
        let searchResult = []; // 검색 결과
        for (let j = 0; j < numwords; j++) { // 가수 검색
            let wordSearch = singer.toLowerCase().search(words[j].toLowerCase());
            if (wordSearch != -1) {
                searchResult.push(wordSearch);
            }
        }
        singer = singer.split("/")[0];
        if (numwords == searchResult.length) { // 가수에서 단어들이 다 검색되면
            searchedSinger += "<tr><td rowspan=\"" + spannum + "\" ";
            if (songsPerRow == 2) {
                searchedSinger += "style=\"width:32%;\"";
            }
            else {
                searchedSinger += "style=\"width:13%;\"";
            }
            searchedSinger += ">" + singer.substring(0, searchResult[0]); // 첫 단어 검색 전
            let pos;
            let index;
            for (let j = 0; j < numwords; j++) {
                if (j == 0) {
                    pos = searchResult[0];
                    index = 0;
                    searchedSinger += "<b>" + singer.substr(pos, strlen[0]) + "</b>"; // 처음 검색된 부분
                }
                else {
                    if (searchResult[j] > pos) {
                        pos = searchResult[j];
                        index = j;
                        searchedSinger += "<b>" + singer.substr(pos, strlen[j]) + "</b>"; // 다음 검색된 부분
                    }
                }
                if (j != numwords - 1) { // 마지막 단어가 아니면
                    if (searchResult[j] + strlen[j] < searchResult[j + 1]) { // 다음 단어 검색 전
                        searchedSinger += singer.substring(searchResult[j] + strlen[j], searchResult[j + 1]);
                    }
                }
            }
            searchedSinger += singer.substr(pos + strlen[index]) + "</td>"; // 마지막 단어 검색 후

            for (let j = 0; j < songslen; j++) {
                let songResult = []; // 가수 노래 검색
                let index = [];
                for (let k = 0; k < numwords; k++) { // 노래에서도 검색되는지
                    let songSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (songSearch != -1) {
                        songResult.push(songSearch);
                        index.push(k);
                    }
                }
                let song = songs[j].split("/")[0];
                let inlen = index.length;
                if (inlen != 0) { // 노래에서 검색된 게 있음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song.substring(0, songResult[0]);
                    let pos;
                    let ind;
                    for (let k = 0; k < inlen; k++) {
                        if (k == 0) {
                            pos = songResult[0];
                            ind = index[0];
                            searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 처음 검색된 부분
                        }
                        else {
                            if (songResult[k] > pos) {
                                pos = songResult[k];
                                ind = index[k];
                                searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 다음 검색된 부분
                            }
                        }
                        if (k != inlen - 1) {
                            let inK = index[k];
                            if (songResult[k] + strlen[inK] < songResult[k + 1]) {
                                searchedSinger += song.substring(songResult[k] + strlen[inK], songResult[k + 1]);
                            }
                        }
                    }
                    searchedSinger += song.substr(pos + strlen[ind]) + "</td>";
                }
                else { // 없음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song + "</td>";
                }
                if (j % songsPerRow == songsPerRow - 1) {
                    searchedSinger += "</tr>";
                    if (j != songslen - 1) {
                        searchedSinger += "<tr>";
                    }
                }
            }
            if (songslen % songsPerRow != 0) {
                searchedSinger += "</tr>";
            }
        }
        else {
            for (let j = 0; j < songslen; j++) {
                searchResult = [];
                for (let k = 0; k < numwords; k++) {
                    let wordSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (wordSearch != -1) {
                        searchResult.push(wordSearch);
                    }
                }
                if (numwords == searchResult.length) {
                    let song = songs[j].split("/")[0];
                    searchedSong += "<tr><td>" + singer + "</td>" + "<td>" + song.substring(0, searchResult[0]);
                    let pos;
                    let index;
                    for (let k = 0; k < numwords; k++) {
                        if (k == 0) {
                            pos = searchResult[0];
                            index = 0;
                            searchedSong += "<b>" + song.substr(pos, strlen[0]) + "</b>";
                        }
                        else {
                            if (searchResult[k] > pos) {
                                pos = searchResult[k];
                                index = k;
                                searchedSong += "<b>" + song.substr(pos, strlen[k]) + "</b>";
                            }
                        }
                        if (k != numwords - 1) {
                            if (searchResult[k] + strlen[k] < searchResult[k + 1]) {
                                searchedSong += song.substring(searchResult[k] + strlen[k], searchResult[k + 1]);
                            }
                        }
                    }
                    searchedSong += song.substr(pos + strlen[index]) + "</td></tr>";
                }
            }
        }
    }
    return { searchedSinger, searchedSong };
}

function mrTrotSearch(numwords, words, searchedSinger, strlen, searchedSong) {
    for (let i = 0; i < mrTrot.length; i++) {
        let singer = mrTrot[i].singer; // 가수
        let songs = mrTrot[i].songs; // 노래들
        let songslen = songs.length; // 노래 수
        let spannum = spanCalc(songslen);
        let searchResult = []; // 검색 결과
        for (let j = 0; j < numwords; j++) { // 가수 검색
            let wordSearch = singer.toLowerCase().search(words[j].toLowerCase());
            if (wordSearch != -1) {
                searchResult.push(wordSearch);
            }
        }
        singer = singer.split("/")[0];
        if (numwords == searchResult.length) { // 가수에서 단어들이 다 검색되면
            searchedSinger += "<tr><td rowspan=\"" + spannum + "\" ";
            if (songsPerRow == 2) {
                searchedSinger += "style=\"width:32%;\"";
            }
            else {
                searchedSinger += "style=\"width:13%;\"";
            }
            searchedSinger += ">" + singer.substring(0, searchResult[0]); // 첫 단어 검색 전
            let pos;
            let index;
            for (let j = 0; j < numwords; j++) {
                if (j == 0) {
                    pos = searchResult[0];
                    index = 0;
                    searchedSinger += "<b>" + singer.substr(pos, strlen[0]) + "</b>"; // 처음 검색된 부분
                }
                else {
                    if (searchResult[j] > pos) {
                        pos = searchResult[j];
                        index = j;
                        searchedSinger += "<b>" + singer.substr(pos, strlen[j]) + "</b>"; // 다음 검색된 부분
                    }
                }
                if (j != numwords - 1) { // 마지막 단어가 아니면
                    if (searchResult[j] + strlen[j] < searchResult[j + 1]) { // 다음 단어 검색 전
                        searchedSinger += singer.substring(searchResult[j] + strlen[j], searchResult[j + 1]);
                    }
                }
            }
            searchedSinger += singer.substr(pos + strlen[index]) + "</td>"; // 마지막 단어 검색 후

            for (let j = 0; j < songslen; j++) {
                let songResult = []; // 가수 노래 검색
                let index = [];
                for (let k = 0; k < numwords; k++) { // 노래에서도 검색되는지
                    let songSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (songSearch != -1) {
                        songResult.push(songSearch);
                        index.push(k);
                    }
                }
                let song = songs[j].split("/")[0];
                let inlen = index.length;
                if (inlen != 0) { // 노래에서 검색된 게 있음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song.substring(0, songResult[0]);
                    let pos;
                    let ind;
                    for (let k = 0; k < inlen; k++) {
                        if (k == 0) {
                            pos = songResult[0];
                            ind = index[0];
                            searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 처음 검색된 부분
                        }
                        else {
                            if (songResult[k] > pos) {
                                pos = songResult[k];
                                ind = index[k];
                                searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 다음 검색된 부분
                            }
                        }
                        if (k != inlen - 1) {
                            let inK = index[k];
                            if (songResult[k] + strlen[inK] < songResult[k + 1]) {
                                searchedSinger += song.substring(songResult[k] + strlen[inK], songResult[k + 1]);
                            }
                        }
                    }
                    searchedSinger += song.substr(pos + strlen[ind]) + "</td>";
                }
                else { // 없음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song + "</td>";
                }
                if (j % songsPerRow == songsPerRow - 1) {
                    searchedSinger += "</tr>";
                    if (j != songslen - 1) {
                        searchedSinger += "<tr>";
                    }
                }
            }
            if (songslen % songsPerRow != 0) {
                searchedSinger += "</tr>";
            }
        }
        else {
            for (let j = 0; j < songslen; j++) {
                searchResult = [];
                for (let k = 0; k < numwords; k++) {
                    let wordSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (wordSearch != -1) {
                        searchResult.push(wordSearch);
                    }
                }
                if (numwords == searchResult.length) {
                    let song = songs[j].split("/")[0];
                    searchedSong += "<tr><td>" + singer + "</td>" + "<td>" + song.substring(0, searchResult[0]);
                    let pos;
                    let index;
                    for (let k = 0; k < numwords; k++) {
                        if (k == 0) {
                            pos = searchResult[0];
                            index = 0;
                            searchedSong += "<b>" + song.substr(pos, strlen[0]) + "</b>";
                        }
                        else {
                            if (searchResult[k] > pos) {
                                pos = searchResult[k];
                                index = k;
                                searchedSong += "<b>" + song.substr(pos, strlen[k]) + "</b>";
                            }
                        }
                        if (k != numwords - 1) {
                            if (searchResult[k] + strlen[k] < searchResult[k + 1]) {
                                searchedSong += song.substring(searchResult[k] + strlen[k], searchResult[k + 1]);
                            }
                        }
                    }
                    searchedSong += song.substr(pos + strlen[index]) + "</td></tr>";
                }
            }
        }
    }
    return { searchedSinger, searchedSong };
}

function trotSearch(numwords, words, searchedSinger, strlen, searchedSong) {
    for (let i = 0; i < trot.length; i++) {
        let singer = trot[i].singer; // 가수
        let songs = trot[i].songs; // 노래들
        let songslen = songs.length; // 노래 수
        let spannum = spanCalc(songslen);
        let searchResult = []; // 검색 결과
        for (let j = 0; j < numwords; j++) { // 가수 검색
            let wordSearch = singer.toLowerCase().search(words[j].toLowerCase());
            if (wordSearch != -1) {
                searchResult.push(wordSearch);
            }
        }
        singer = singer.split("/")[0];
        if (numwords == searchResult.length) { // 가수에서 단어들이 다 검색되면
            searchedSinger += "<tr><td rowspan=\"" + spannum + "\" ";
            if (songsPerRow == 2) {
                searchedSinger += "style=\"width:32%;\"";
            }
            else {
                searchedSinger += "style=\"width:13%;\"";
            }
            searchedSinger += ">" + singer.substring(0, searchResult[0]); // 첫 단어 검색 전
            let pos;
            let index;
            for (let j = 0; j < numwords; j++) {
                if (j == 0) {
                    pos = searchResult[0];
                    index = 0;
                    searchedSinger += "<b>" + singer.substr(pos, strlen[0]) + "</b>"; // 처음 검색된 부분
                }
                else {
                    if (searchResult[j] > pos) {
                        pos = searchResult[j];
                        index = j;
                        searchedSinger += "<b>" + singer.substr(pos, strlen[j]) + "</b>"; // 다음 검색된 부분
                    }
                }
                if (j != numwords - 1) { // 마지막 단어가 아니면
                    if (searchResult[j] + strlen[j] < searchResult[j + 1]) { // 다음 단어 검색 전
                        searchedSinger += singer.substring(searchResult[j] + strlen[j], searchResult[j + 1]);
                    }
                }
            }
            searchedSinger += singer.substr(pos + strlen[index]) + "</td>"; // 마지막 단어 검색 후

            for (let j = 0; j < songslen; j++) {
                let songResult = []; // 가수 노래 검색
                let index = [];
                for (let k = 0; k < numwords; k++) { // 노래에서도 검색되는지
                    let songSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (songSearch != -1) {
                        songResult.push(songSearch);
                        index.push(k);
                    }
                }
                let song = songs[j].split("/")[0];
                let inlen = index.length;
                if (inlen != 0) { // 노래에서 검색된 게 있음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song.substring(0, songResult[0]);
                    let pos;
                    let ind;
                    for (let k = 0; k < inlen; k++) {
                        if (k == 0) {
                            pos = songResult[0];
                            ind = index[0];
                            searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 처음 검색된 부분
                        }
                        else {
                            if (songResult[k] > pos) {
                                pos = songResult[k];
                                ind = index[k];
                                searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 다음 검색된 부분
                            }
                        }
                        if (k != inlen - 1) {
                            let inK = index[k];
                            if (songResult[k] + strlen[inK] < songResult[k + 1]) {
                                searchedSinger += song.substring(songResult[k] + strlen[inK], songResult[k + 1]);
                            }
                        }
                    }
                    searchedSinger += song.substr(pos + strlen[ind]) + "</td>";
                }
                else { // 없음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song + "</td>";
                }
                if (j % songsPerRow == songsPerRow - 1) {
                    searchedSinger += "</tr>";
                    if (j != songslen - 1) {
                        searchedSinger += "<tr>";
                    }
                }
            }
            if (songslen % songsPerRow != 0) {
                searchedSinger += "</tr>";
            }
        }
        else {
            for (let j = 0; j < songslen; j++) {
                searchResult = [];
                for (let k = 0; k < numwords; k++) {
                    let wordSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (wordSearch != -1) {
                        searchResult.push(wordSearch);
                    }
                }
                if (numwords == searchResult.length) {
                    let song = songs[j].split("/")[0];
                    searchedSong += "<tr><td>" + singer + "</td>" + "<td>" + song.substring(0, searchResult[0]);
                    let pos;
                    let index;
                    for (let k = 0; k < numwords; k++) {
                        if (k == 0) {
                            pos = searchResult[0];
                            index = 0;
                            searchedSong += "<b>" + song.substr(pos, strlen[0]) + "</b>";
                        }
                        else {
                            if (searchResult[k] > pos) {
                                pos = searchResult[k];
                                index = k;
                                searchedSong += "<b>" + song.substr(pos, strlen[k]) + "</b>";
                            }
                        }
                        if (k != numwords - 1) {
                            if (searchResult[k] + strlen[k] < searchResult[k + 1]) {
                                searchedSong += song.substring(searchResult[k] + strlen[k], searchResult[k + 1]);
                            }
                        }
                    }
                    searchedSong += song.substr(pos + strlen[index]) + "</td></tr>";
                }
            }
        }
    }
    return { searchedSinger, searchedSong };
}

function titleSearch(numwords, words, searchedSinger, strlen, searchedSong) {
    for (let i = 0; i < title.length; i++) {
        let singer = title[i].singer; // 가수
        let songs = title[i].songs; // 노래들
        let songslen = songs.length; // 노래 수
        let spannum = spanCalc(songslen);
        let searchResult = []; // 검색 결과
        for (let j = 0; j < numwords; j++) { // 가수 검색
            let wordSearch = singer.toLowerCase().search(words[j].toLowerCase());
            if (wordSearch != -1) {
                searchResult.push(wordSearch);
            }
        }
        singer = singer.split("/")[0];
        if (numwords == searchResult.length) { // 가수에서 단어들이 다 검색되면
            searchedSinger += "<tr><td rowspan=\"" + spannum + "\" ";
            if (songsPerRow == 2) {
                searchedSinger += "style=\"width:32%;\"";
            }
            else {
                searchedSinger += "style=\"width:13%;\"";
            }
            searchedSinger += ">" + singer.substring(0, searchResult[0]); // 첫 단어 검색 전
            let pos;
            let index;
            for (let j = 0; j < numwords; j++) {
                if (j == 0) {
                    pos = searchResult[0];
                    index = 0;
                    searchedSinger += "<b>" + singer.substr(pos, strlen[0]) + "</b>"; // 처음 검색된 부분
                }
                else {
                    if (searchResult[j] > pos) {
                        pos = searchResult[j];
                        index = j;
                        searchedSinger += "<b>" + singer.substr(pos, strlen[j]) + "</b>"; // 다음 검색된 부분
                    }
                }
                if (j != numwords - 1) { // 마지막 단어가 아니면
                    if (searchResult[j] + strlen[j] < searchResult[j + 1]) { // 다음 단어 검색 전
                        searchedSinger += singer.substring(searchResult[j] + strlen[j], searchResult[j + 1]);
                    }
                }
            }
            searchedSinger += singer.substr(pos + strlen[index]) + "</td>"; // 마지막 단어 검색 후

            for (let j = 0; j < songslen; j++) {
                let songResult = []; // 가수 노래 검색
                let index = [];
                for (let k = 0; k < numwords; k++) { // 노래에서도 검색되는지
                    let songSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (songSearch != -1) {
                        songResult.push(songSearch);
                        index.push(k);
                    }
                }
                let song = songs[j].split("/")[0];
                let inlen = index.length;
                if (inlen != 0) { // 노래에서 검색된 게 있음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song.substring(0, songResult[0]);
                    let pos;
                    let ind;
                    for (let k = 0; k < inlen; k++) {
                        if (k == 0) {
                            pos = songResult[0];
                            ind = index[0];
                            searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 처음 검색된 부분
                        }
                        else {
                            if (songResult[k] > pos) {
                                pos = songResult[k];
                                ind = index[k];
                                searchedSinger += "<b>" + song.substr(pos, strlen[ind]) + "</b>"; // 다음 검색된 부분
                            }
                        }
                        if (k != inlen - 1) {
                            let inK = index[k];
                            if (songResult[k] + strlen[inK] < songResult[k + 1]) {
                                searchedSinger += song.substring(songResult[k] + strlen[inK], songResult[k + 1]);
                            }
                        }
                    }
                    searchedSinger += song.substr(pos + strlen[ind]) + "</td>";
                }
                else { // 없음
                    searchedSinger += "<td ";
                    if (songsPerRow == 2) {
                        searchedSinger += "style=\"width:34%;\"";
                    }
                    else {
                        searchedSinger += "style=\"width:14.5%;\"";
                    }
                    searchedSinger += ">" + song + "</td>";
                }
                if (j % songsPerRow == songsPerRow - 1) {
                    searchedSinger += "</tr>";
                    if (j != songslen - 1) {
                        searchedSinger += "<tr>";
                    }
                }
            }
            if (songslen % songsPerRow != 0) {
                searchedSinger += "</tr>";
            }
        }
        else {
            for (let j = 0; j < songslen; j++) {
                searchResult = [];
                for (let k = 0; k < numwords; k++) {
                    let wordSearch = songs[j].toLowerCase().search(words[k].toLowerCase());
                    if (wordSearch != -1) {
                        searchResult.push(wordSearch);
                    }
                }
                if (numwords == searchResult.length) {
                    let song = songs[j].split("/")[0];
                    searchedSong += "<tr><td>" + singer + "</td>" + "<td>" + song.substring(0, searchResult[0]);
                    let pos;
                    let index;
                    for (let k = 0; k < numwords; k++) {
                        if (k == 0) {
                            pos = searchResult[0];
                            index = 0;
                            searchedSong += "<b>" + song.substr(pos, strlen[0]) + "</b>";
                        }
                        else {
                            if (searchResult[k] > pos) {
                                pos = searchResult[k];
                                index = k;
                                searchedSong += "<b>" + song.substr(pos, strlen[k]) + "</b>";
                            }
                        }
                        if (k != numwords - 1) {
                            if (searchResult[k] + strlen[k] < searchResult[k + 1]) {
                                searchedSong += song.substring(searchResult[k] + strlen[k], searchResult[k + 1]);
                            }
                        }
                    }
                    searchedSong += song.substr(pos + strlen[index]) + "</td></tr>";
                }
            }
        }
    }
    return { searchedSinger, searchedSong };
}
